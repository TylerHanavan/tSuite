CREATE TABLE commits (id INT NOT NULL AUTO_INCREMENT PRIMARY KEY, repo INT NOT NULL, commit_hash VARCHAR(40) NOT NULL, date DATETIME NOT NULL, author VARCHAR(100) NOT NULL, message TEXT NOT NULL, test_status TINYINT, success_tests INT, failed_tests INT, download_duration INT, install_duration INT, test_duration INT);